name: E2E Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch: # Allow manual trigger

# Cancel superseded runs on the same ref to save CI minutes.
concurrency:
  group: e2e-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  NODE_VERSION: '22'
  PNPM_VERSION: '9.15.9'

jobs:
  # Primary gate: the full Playwright suite on Chromium against a real,
  # seeded backend (the app's primary target browser).
  e2e:
    name: E2E Tests (Chromium)
    runs-on: ubuntu-latest
    timeout-minutes: 40
    permissions:
      contents: read
      issues: write # post the screenshot-summary comment on PRs (issues/{n}/comments)
      pull-requests: write # read the issues/PR conversations if needed

    # The API reads these from the shell (its dotenv path is build-relative and
    # unreliable), so we set them job-wide rather than via a .env file.
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/cipansor_test
      REDIS_URL: redis://localhost:6379
      JWT_SECRET: test-jwt-secret-key-for-e2e-testing
      # Production default is 15m; e2e pins 7d because global-setup logs each
      # role in ONCE and reuses the tokens for the whole (up to 40-min) run.
      JWT_EXPIRES_IN: 7d
      JWT_REFRESH_EXPIRES_IN: 30d
      CORS_ORIGIN: http://localhost:3000
      PORT: '3001'
      LOG_LEVEL: error
      # The e2e auth helper signs admin TOTPs from a known secret seeded when
      # E2E_FIXED_2FA=1; raise the 2FA limiter so the many logins don't trip it.
      TWO_FACTOR_RATE_LIMIT_MAX: '100000'
      # Same reason for the e-sign limiter: a spec that signs several letters
      # in one run would otherwise hit a cap meant to slow down guessing.
      ESIGN_RATE_LIMIT_MAX: '100000'
      # Baked into the web build: API base origin only (the app appends /api).
      NEXT_PUBLIC_API_URL: http://localhost:3001
      # Used by the e2e test runner, which builds full paths — keep the /api.
      API_URL: http://localhost:3001/api
      BASE_URL: http://localhost:3000

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: cipansor_test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@v6.1.0
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Playwright (Chromium)
        run: pnpm --filter web exec playwright install --with-deps chromium

      - name: Generate Prisma client
        run: pnpm --filter api db:generate

      - name: Push database schema
        run: pnpm --filter api db:push

      - name: Seed database (with fixed-secret 2FA for admins)
        run: pnpm --filter api db:seed
        env:
          E2E_FIXED_2FA: '1'

      - name: Build shared + API + Web
        run: |
          pnpm --filter @cipansor/shared build
          pnpm --filter api build
          pnpm --filter web build
        env:
          # tsc on the full API surface exceeds Node's ~2GB default heap and
          # OOMs (exit 134); raise the limit (runners have plenty of RAM).
          NODE_OPTIONS: --max-old-space-size=4096

      - name: Start API and wait for health
        run: |
          cd apps/api
          node dist/main.js > /tmp/api.log 2>&1 &
          echo $! > /tmp/api.pid
          timeout 90 bash -c 'until curl -fs http://localhost:3001/health >/dev/null; do sleep 2; done' \
            || { echo "API did not become healthy"; cat /tmp/api.log; exit 1; }
          echo "API is healthy"

      # Playwright's webServer starts the production web server (`pnpm start`)
      # in CI; the API is already running above.
      - name: Run E2E tests
        run: pnpm --filter web exec playwright test --project=chromium

      - name: Stop API
        if: always()
        run: '[ -f /tmp/api.pid ] && kill "$(cat /tmp/api.pid)" || true'

      # Artifact names are stable per ref: on a PR it's e2e-screenshots-pr-<n>
      # (so concurrent runs on different PRs don't collide and `overwrite: true`
      # replaces this PR's previous run's screenshots instead of accumulating);
      # on a push to main ref_name="main" keeps a single e2e-screenshots-main.
      # The full set of screenshots (passed + failed) lives here; the PR comment
      # lists them by filename and links to this artifact.
      - name: Upload E2E screenshots (passed + failed)
        id: upload-screenshots
        if: always()
        continue-on-error: true # storage-quota issues must not fail the gate
        uses: actions/upload-artifact@v7
        with:
          name: e2e-screenshots-${{ github.event.pull_request.number || github.ref_name }}
          path: apps/web/test-results/
          retention-days: 7
          overwrite: true
          if-no-files-found: warn

      - name: Upload Playwright report
        id: upload-report
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          name: playwright-report-${{ github.event.pull_request.number || github.ref_name }}
          path: apps/web/playwright-report/
          retention-days: 7
          overwrite: true
          if-no-files-found: warn

      # Build the comment with the two screenshot groups and link the artifacts.
      # Uses GITHUB_TOKEN and `issues: write` (permission set on the job).
      - name: Comment PR with E2E screenshots
        if: ${{ always() && github.event_name == 'pull_request' }}
        continue-on-error: true # cosmetic comment — never fail the gate
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPOSITORY: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          RESULT_JSON: apps/web/test-results/results.json
          SCREENSHOT_ARTIFACT_URL: ${{ steps.upload-screenshots.outputs.artifact-url }}
          REPORT_ARTIFACT_URL: ${{ steps.upload-report.outputs.artifact-url }}
        run: node scripts/e2e-comment.mjs

  # Supplementary cross-browser coverage on main. Firefox/WebKit are not the
  # primary target and can surface engine-specific quirks, so this is reported
  # but does not block the pipeline (flip continue-on-error off to gate on it).
  e2e-cross-browser:
    name: E2E Tests (${{ matrix.browser }})
    runs-on: ubuntu-latest
    if: ${{ github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && github.ref == 'refs/heads/main') }}
    timeout-minutes: 45
    continue-on-error: true
    strategy:
      fail-fast: false
      matrix:
        browser: [firefox, webkit]

    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/cipansor_test
      REDIS_URL: redis://localhost:6379
      JWT_SECRET: test-jwt-secret-key-for-e2e-testing
      # Production default is 15m; e2e pins 7d because global-setup logs each
      # role in ONCE and reuses the tokens for the whole (up to 40-min) run.
      JWT_EXPIRES_IN: 7d
      JWT_REFRESH_EXPIRES_IN: 30d
      CORS_ORIGIN: http://localhost:3000
      PORT: '3001'
      LOG_LEVEL: error
      TWO_FACTOR_RATE_LIMIT_MAX: '100000'
      ESIGN_RATE_LIMIT_MAX: '100000'
      NEXT_PUBLIC_API_URL: http://localhost:3001
      API_URL: http://localhost:3001/api
      BASE_URL: http://localhost:3000

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: cipansor_test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@v6.1.0
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Playwright (${{ matrix.browser }})
        run: pnpm --filter web exec playwright install --with-deps ${{ matrix.browser }}

      - name: Generate Prisma client
        run: pnpm --filter api db:generate

      - name: Push database schema
        run: pnpm --filter api db:push

      - name: Seed database (with fixed-secret 2FA for admins)
        run: pnpm --filter api db:seed
        env:
          E2E_FIXED_2FA: '1'

      - name: Build shared + API + Web
        run: |
          pnpm --filter @cipansor/shared build
          pnpm --filter api build
          pnpm --filter web build
        env:
          NODE_OPTIONS: --max-old-space-size=4096

      - name: Start API and wait for health
        run: |
          cd apps/api
          node dist/main.js > /tmp/api.log 2>&1 &
          echo $! > /tmp/api.pid
          timeout 90 bash -c 'until curl -fs http://localhost:3001/health >/dev/null; do sleep 2; done' \
            || { echo "API did not become healthy"; cat /tmp/api.log; exit 1; }

      - name: Run E2E tests (${{ matrix.browser }})
        run: pnpm --filter web exec playwright test --project=${{ matrix.browser }}

      - name: Stop API
        if: always()
        run: '[ -f /tmp/api.pid ] && kill "$(cat /tmp/api.pid)" || true'

      - name: Upload Playwright report
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          name: playwright-report-${{ matrix.browser }}
          path: apps/web/playwright-report/
          retention-days: 7
